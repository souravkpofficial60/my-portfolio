(
echo name: Deploy Portfolio
echo.
echo on:
echo   push:
echo     branches: [main]
echo.
echo jobs:
echo   deploy:
echo     name: Deploy to GitHub Pages
echo     runs-on: ubuntu-latest
echo     permissions:
echo       contents: write
echo     steps:
echo       - name: Checkout code
echo         uses: actions/checkout@v4
echo       - name: Deploy to GitHub Pages
echo         uses: peaceiris/actions-gh-pages@v4
echo         with:
echo           github_token: ${{ secrets.GITHUB_TOKEN }}
echo           publish_dir: ./
echo           exclude_assets: '.github,backend,node_modules'
) > ".github\workflows\deploy.yml"